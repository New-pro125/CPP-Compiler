int main() {
    int a, g, f;
    a = g = f = 42;
    a += g -= f *= 2;
    float y = 5.25;
    int x = 8;
    int z = 7;
    char c = 'a';
    bool tr = true;
    x +=y;
    x -=y;
    x *=y;
    x /=y;
    x %=z;
    x <<=z;
    x >>=z;
    x |=z;
    x &=z;
    x ^=z;
    x = x + c;
    x = x - 8;
    x = x * 9;
    x = x / c;
    x = x % 8;
    x = x << 1;
    x = x >> tr;
    x = x | tr;
    x = x & c;
    x = x ^ 9;
    string w = "g";
    string v = "d" + "e";
    x++;
    ++x;
    x--;
    --x;
    bool b = x > 8;
    b = x < 8;
    b = x == 0;
    b = x!= 6;
    b = x == 0 && x != 7;
    b = x !=8 || x < 8;
    int u = ~x;
    bool bc = !tr;
}
